import '../../../../core/core_importer.dart';

class TransactionRequestWidget extends StatelessWidget {
  const TransactionRequestWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
